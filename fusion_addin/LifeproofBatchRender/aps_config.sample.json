{
  "client_id": "YOUR_APS_CLIENT_ID",
  "client_secret": "YOUR_APS_CLIENT_SECRET",
  "scopes": "data:read data:write bucket:create bucket:read",
  "bucket_key": "your-unique-lowercase-bucket-name",
  "bucket_policy": "transient",
  "auto_create_bucket": true,
  "render_mode": "fusion_viewport",
  "render_quality": 90,
  "max_retries": 2,
  "workflow_notes": "Copy to aps_config.json (gitignored). render_mode: fusion_viewport (recommended) uploads job package to OSS then viewport-captures in Fusion; fusion_raytrace is slow on decal-heavy models; automation requires Fusion Automation API setup."
}
